from math import ceil
#принимает в качестве аргумента целое число и выводит True если оно простое
def is_prime(num): #работает с числами >=3 
    flag = True
    for k in range(2, limit + 1):
        if num % k == 0:
            flag = False
            break
    return flag
